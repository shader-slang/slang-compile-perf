# Derived HTML/SVG reports — regenerated locally from results.json on demand.
# Not committed; served via GitHub Pages from the gh-pages branch.
analysis/
sweep/
breakdown/

# analyze.py local side outputs
flags.csv
series.csv
